{
  "name": "apollo-platform",
  "private": true,
  "workspaces": ["packages/*"],
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
